<%= form_for @document_state do |form| %>
  <% if @document_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@document_state.errors.count, "error") %> prohibited this document_state from being saved:</h2>

      <ul>
        <% @document_state.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name, "Nombre" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :format, "Formato" %>
    <%= form.text_field :format, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :icon, "Icono" %>
    <%= form.text_field :icon, class: "form-control" %>
  </div>

  <div class="form-group">
  <%= form.submit "Guardar", :class => "btn btn-primary" %>
  <%= link_to "Volver", :back, :class => "btn btn-secondary" %>
  </div>
<% end %>
