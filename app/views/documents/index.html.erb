<h1>Lista de Documentos</h1>
<table class="table">
  <colgroup>
    <col width="25%"/>
    <col width="25%"/>
    <col width="25%"/>
    <col width="25%"/>
    <col/>
    <col/>
    <col/>
  </colgroup>
  <thead class="thead-dark">
    <tr>
      <th scope="col">Código</th>
      <th scope="col">Nro. Documento</th>
      <th scope="col">Monto Total</th>
      <th scope="col">Estado</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @documents.each do |d| %>
    <tr>
      <td><%= d.code %></td>
      <td><%= d.doc_code %></td>
      <td><%= d.total_amount %></td>
      <td><%= d.state %></td>
      <td><%= link_to 'Show', :action => :show, :id => d.id %></td>
      <td><%= link_to 'Edit', :action => :edit, :id => d.id %></td>
      <td><%= link_to 'Destroy', { :action => :destroy, :id => d.id }, :method => :delete, :data => { :confirm => 'Estas Seguro?' } %></td>
    </tr>
    <% end %>
  </tbody>
</table>
<%= link_to "Crear", :action => :new %>
